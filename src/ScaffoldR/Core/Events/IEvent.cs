namespace ScaffoldR.Core.Events
{
    /// <summary>
    /// Specifices that the target class can be notified by implemented <see cref="IHandleEvent{TEvent}"/>. This is a marker interface and has no methods.
    /// </summary>
    public interface IEvent
    {
        // Marker interface
    }
}
